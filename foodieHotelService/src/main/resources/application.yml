server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/foodieService
    username: root
    password: Drc@1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: FOODIE-HOTEL-SERVICE
  config:
    import: optional:configserver:http://localhost:8085

